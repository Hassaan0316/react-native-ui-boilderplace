import { ViewStyle } from 'react-native';
interface iProps extends ViewStyle {
    indicatorColor?: string;
}
declare const FullPageLoader: (props: iProps) => import("react/jsx-runtime").JSX.Element;
export default FullPageLoader;
